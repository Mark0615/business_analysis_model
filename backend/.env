# 允許的前端網域（逗號分隔）
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# 上限：請求大小（bytes）/ 每次分析最多筆數
REQUEST_MAX_BYTES=10485760
MAX_ROWS=200000

# 單項演算法逾時秒數（Apriori / RFM）
ALGO_TIMEOUT_SEC=15

# 強制 HTTPS（部署到 Cloud Run / Render 等代理後建議開啟）
REQUIRE_HTTPS=0

# 日誌層級：DEBUG/INFO/WARN/ERROR
LOG_LEVEL=INFO

# Sentry（選填）
# SENTRY_DSN=https://xxxxxxx.ingest.sentry.io/123456